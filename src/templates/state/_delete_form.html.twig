{#<form method="post" action="{{ path('state_delete', {'id': state.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">#}
{#    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ state.id) }}">#}
{#    <button class="btn">Delete</button>#}
{#</form>#}
<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ state.id) }}">
<button class="btn-top" data-toggle="modal"
        data-target="#modal_delete{{ state.id }}">Delete
</button>
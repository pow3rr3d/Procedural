{% form_theme form _self %}

{{ form_start(form) }}
    {{ form_row(form.name) }}
    {{ form_row(form.description) }}
    {{ form_row(form.steps) }}
    <button class="btn-top">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
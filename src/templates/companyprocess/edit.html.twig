{% extends 'base.html.twig' %}

{% block title %}Edit Company Process{% endblock %}

{% block body %}
    <section class="wrapper">
        <div class="button-div">
            <a class="btn-top" href="{{ path('allprocess_index') }}">back to list</a>
        </div>
        {{ include('companyprocess/_form.html.twig', {'button_label': 'Update'}) }}
    </section>
{% endblock %}
